<style>
/* --- Landing Page Styles --- */
.nitted-landing {
  font-family: 'Inter', 'Satoshi', 'Manrope', Arial, sans-serif;
  background: #FAF5EF;
  min-height: 100vh;
  margin: 0;
  padding: 0;
}
.nitted-hero {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}
@media (min-width: 900px) {
  .nitted-hero {
    flex-direction: row;
  }
}
.nitted-hero-left {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  padding: 48px 24px 24px 24px;
  background: #EFE9E1;
}
.nitted-logo-img {
  width: 180px;
  max-width: 80vw;
  margin-bottom: 2rem;
  display: block;
}
.nitted-hero-date {
  font-size: 2rem;
  font-weight: 700;
  margin-bottom: 1.5rem;
  text-align: center;
}
.nitted-countdown {
  display: flex;
  justify-content: center;
  gap: 1.5rem;
  background: #fff8;
  border-radius: 1rem;
  padding: 1rem 2.5rem;
  font-size: 2rem;
  font-family: monospace;
  font-weight: 700;
  box-shadow: 0 2px 12px 0 #0001;
  margin-bottom: 2rem;
}
.nitted-hero-sub {
  font-size: 1.5rem;
  font-weight: 600;
  margin-bottom: 1rem;
  text-align: center;
}
.nitted-hero-desc {
  font-size: 1.1rem;
  color: #555;
  margin-bottom: 2rem;
  text-align: center;
}
.nitted-form {
  width: 100%;
  max-width: 400px;
  margin: 0 auto 1.5rem auto;
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}
.nitted-form input[type="email"] {
  width: 100%;
  padding: 1rem;
  border-radius: 0.75rem;
  border: 1px solid #ccc;
  font-size: 1.1rem;
  outline: none;
  background: #fff;
  box-shadow: 0 1px 4px #0001;
}
.nitted-form button {
  width: 100%;
  background: #111;
  color: #fff;
  border: none;
  border-radius: 0.75rem;
  padding: 1rem;
  font-size: 1.1rem;
  font-weight: 700;
  cursor: pointer;
  transition: background 0.2s;
}
.nitted-form button:hover {
  background: #333;
}
.nitted-form-caption {
  font-size: 0.95rem;
  color: #666;
  text-align: center;
}
.nitted-hero-right {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #EFE9E1;
  min-height: 350px;
  padding: 24px 0;
}
.nitted-model-viewer {
  width: 100%;
  max-width: 480px;
  height: 480px;
  background: #fff;
  border-radius: 1.5rem;
  box-shadow: 0 2px 24px 0 #0002;
}
@media (max-width: 899px) {
  .nitted-hero-right {
    min-height: 320px;
    padding: 0 0 24px 0;
  }
  .nitted-model-viewer {
    max-width: 340px;
    height: 340px;
  }
}
.nitted-carousel {
  display: flex;
  justify-content: center;
  align-items: flex-end;
  gap: 2rem;
  margin: 3rem auto 0 auto;
  max-width: 900px;
  width: 100%;
  min-height: 340px;
  position: relative;
}
.nitted-carousel-img {
  transition: all 0.5s cubic-bezier(.4,0,.2,1);
  border-radius: 1rem;
  box-shadow: 0 2px 12px 0 #0001;
  background: #fff;
  object-fit: cover;
  position: relative;
  z-index: 1;
}
.nitted-carousel-img.center {
  width: 340px;
  height: 340px;
  filter: none;
  z-index: 2;
  box-shadow: 0 4px 32px 0 #0002;
}
.nitted-carousel-img.side {
  width: 180px;
  height: 180px;
  filter: blur(2.5px) brightness(1.05);
  opacity: 0.7;
  transform: scale(0.92) translateY(30px);
  z-index: 1;
}
@media (max-width: 899px) {
  .nitted-carousel {
    gap: 0.5rem;
    min-height: 200px;
  }
  .nitted-carousel-img.center {
    width: 180px;
    height: 180px;
  }
  .nitted-carousel-img.side {
    width: 90px;
    height: 90px;
    transform: scale(0.92) translateY(12px);
  }
}
</style>

<section class="nitted-landing">
  <div class="nitted-hero">
    <!-- Left: Logo, Text & Form -->
    <div class="nitted-hero-left">
      <img src="{{ 'logo.png' | asset_url }}" alt="nitted logo" class="nitted-logo-img" />
      <div class="nitted-hero-date">Dropping August 1st</div>
      <div class="nitted-countdown" id="nitted-countdown">
        <span><span id="nitted-days">00</span> <span style="font-size:0.8rem;">DAYS</span></span>
        <span>:</span>
        <span><span id="nitted-hours">00</span> <span style="font-size:0.8rem;">HRS</span></span>
        <span>:</span>
        <span><span id="nitted-minutes">00</span> <span style="font-size:0.8rem;">MIN</span></span>
        <span>:</span>
        <span><span id="nitted-seconds">00</span> <span style="font-size:0.8rem;">SEC</span></span>
      </div>
      <div class="nitted-hero-sub">The Coziest Drop Is Coming ☁️</div>
      <div class="nitted-hero-desc">Minimal essentials for comfort, calm, and everyday softness.</div>
      <form class="nitted-form">
        <input type="email" placeholder="Enter your email" required />
        <button type="submit">Join the Waitlist →</button>
        <div class="nitted-form-caption">You'll get first access & a secret discount 👀</div>
      </form>
    </div>
    <!-- Right: 3D Model -->
    <div class="nitted-hero-right">
      <model-viewer
        src="{{ 'shirt3dmodel.glb' | asset_url }}"
        alt="3D model"
        auto-rotate
        camera-controls
        ar
        class="nitted-model-viewer"
        style="background: #fff;"
        exposure="1.1"
        shadow-intensity="0.7"
      ></model-viewer>
    </div>
  </div>
  <!-- Product Carousel -->
  <div class="nitted-carousel">
    <img id="carousel-left" class="nitted-carousel-img side" src="{{ 'product-photo2.png' | asset_url }}" alt="Shirt Side" />
    <img id="carousel-center" class="nitted-carousel-img center" src="{{ 'product-photo1.png' | asset_url }}" alt="Shirt Center" />
    <img id="carousel-right" class="nitted-carousel-img side" src="{{ 'product-photo2.png' | asset_url }}" alt="Shirt Side" />
  </div>
</section>

<script type="module" src="https://unpkg.com/@google/model-viewer/dist/model-viewer.min.js"></script>
<script>
// Countdown Timer to August 1, 2025
(function() {
  function pad(n) { return n < 10 ? '0' + n : n; }
  function updateCountdown() {
    var target = new Date('2025-08-01T00:00:00Z');
    var now = new Date();
    var diff = target - now;
    if (diff < 0) diff = 0;
    var days = Math.floor(diff / (1000 * 60 * 60 * 24));
    var hours = Math.floor((diff / (1000 * 60 * 60)) % 24);
    var minutes = Math.floor((diff / (1000 * 60)) % 60);
    var seconds = Math.floor((diff / 1000) % 60);
    document.getElementById('nitted-days').textContent = pad(days);
    document.getElementById('nitted-hours').textContent = pad(hours);
    document.getElementById('nitted-minutes').textContent = pad(minutes);
    document.getElementById('nitted-seconds').textContent = pad(seconds);
  }
  updateCountdown();
  setInterval(updateCountdown, 1000);
})();

// Coverflow-style carousel logic
(function() {
  // You can add more images here later
  var images = [
    { src: "{{ 'product-photo1.png' | asset_url }}", alt: "Shirt 1" },
    { src: "{{ 'product-photo2.png' | asset_url }}", alt: "Shirt 2" }
  ];
  var idx = 0;
  function updateCarousel() {
    var left = document.getElementById('carousel-left');
    var center = document.getElementById('carousel-center');
    var right = document.getElementById('carousel-right');
    var leftIdx = (idx + images.length - 1) % images.length;
    var rightIdx = (idx + 1) % images.length;
    left.src = images[leftIdx].src;
    left.alt = images[leftIdx].alt + ' Side';
    center.src = images[idx].src;
    center.alt = images[idx].alt + ' Center';
    right.src = images[rightIdx].src;
    right.alt = images[rightIdx].alt + ' Side';
  }
  setInterval(function() {
    idx = (idx + 1) % images.length;
    updateCarousel();
  }, 2500);
  updateCarousel();
})();
</script>

