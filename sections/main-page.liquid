<style>
/* --- Landing Page Styles --- */
.nitted-landing {
  font-family: 'Inter', 'Satoshi', 'Manrope', Arial, sans-serif;
  background: #FAF5EF;
  min-height: 100vh;
  margin: 0;
  padding: 0;
}
.nitted-hero {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}
@media (min-width: 900px) {
  .nitted-hero {
    flex-direction: row;
  }
}
.nitted-hero-left {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  padding: 48px 24px 24px 24px;
  background: #EFE9E1;
}
.nitted-logo-img {
  width: 180px;
  max-width: 80vw;
  margin-bottom: 2rem;
  display: block;
}
.nitted-hero-date {
  font-size: 2rem;
  font-weight: 700;
  margin-bottom: 1.5rem;
  text-align: center;
}
.nitted-countdown {
  display: flex;
  justify-content: center;
  gap: 1.5rem;
  background: #fff8;
  border-radius: 1rem;
  padding: 1rem 2.5rem;
  font-size: 2rem;
  font-family: monospace;
  font-weight: 700;
  box-shadow: 0 2px 12px 0 #0001;
  margin-bottom: 2rem;
}
.nitted-hero-sub {
  font-size: 1.5rem;
  font-weight: 600;
  margin-bottom: 1rem;
  text-align: center;
}
.nitted-hero-desc {
  font-size: 1.1rem;
  color: #555;
  margin-bottom: 2rem;
  text-align: center;
}
.nitted-form {
  width: 100%;
  max-width: 400px;
  margin: 0 auto 1.5rem auto;
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}
.nitted-form input[type="email"] {
  width: 100%;
  padding: 1rem;
  border-radius: 0.75rem;
  border: 1px solid #ccc;
  font-size: 1.1rem;
  outline: none;
  background: #fff;
  box-shadow: 0 1px 4px #0001;
}
.nitted-form button {
  width: 100%;
  background: #111;
  color: #fff;
  border: none;
  border-radius: 0.75rem;
  padding: 1rem;
  font-size: 1.1rem;
  font-weight: 700;
  cursor: pointer;
  transition: background 0.2s;
}
.nitted-form button:hover {
  background: #333;
}
.nitted-form-caption {
  font-size: 0.95rem;
  color: #666;
  text-align: center;
}
.nitted-hero-right {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #EFE9E1;
  min-height: 350px;
  padding: 24px 0;
}
.nitted-rotating-container {
  width: 100%;
  max-width: 480px;
  height: 480px;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}
.nitted-rotating-image {
  position: absolute;
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 1.5rem;
  box-shadow: 0 2px 24px 0 #0002;
  opacity: 0;
  transition: opacity 0.5s ease-in-out;
}
.nitted-rotating-image.active {
  opacity: 1;
}
.nitted-dots {
  position: absolute;
  bottom: 1rem;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  gap: 0.5rem;
  z-index: 10;
}
.nitted-dot {
  width: 0.5rem;
  height: 0.5rem;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.5);
  cursor: pointer;
  transition: all 0.3s ease;
}
.nitted-dot.active {
  background: white;
  transform: scale(1.25);
}
@media (max-width: 899px) {
  .nitted-hero-right {
    min-height: 320px;
    padding: 0 0 24px 0;
  }
  .nitted-rotating-container {
    max-width: 340px;
    height: 340px;
  }
}
</style>

<section class="nitted-landing">
  <div class="nitted-hero">
    <!-- Left: Logo, Text & Form -->
    <div class="nitted-hero-left">
      <img src="{{ 'logo.png' | asset_url }}" alt="nitted logo" class="nitted-logo-img" />
      <div class="nitted-hero-date">Dropping August 1st</div>
      <div class="nitted-countdown" id="nitted-countdown">
        <span><span id="nitted-days">00</span> <span style="font-size:0.8rem;">DAYS</span></span>
        <span>:</span>
        <span><span id="nitted-hours">00</span> <span style="font-size:0.8rem;">HRS</span></span>
        <span>:</span>
        <span><span id="nitted-minutes">00</span> <span style="font-size:0.8rem;">MIN</span></span>
        <span>:</span>
        <span><span id="nitted-seconds">00</span> <span style="font-size:0.8rem;">SEC</span></span>
      </div>
      <div class="nitted-hero-sub">The Coziest Drop Is Coming ☁️</div>
      <div class="nitted-hero-desc">Minimal essentials for comfort, calm, and everyday softness.</div>
      <form class="nitted-form">
        <input type="email" placeholder="Enter your email" required />
        <button type="submit">Join the Waitlist →</button>
        <div class="nitted-form-caption">You'll get first access & a secret discount 👀</div>
      </form>
    </div>
    <!-- Right: Rotating Product Images -->
    <div class="nitted-hero-right">
      <div class="nitted-rotating-container">
        <img src="{{ 'product-photo1.png' | asset_url }}" alt="Product 1" class="nitted-rotating-image active" data-index="0" />
        <img src="{{ 'product-photo2.png' | asset_url }}" alt="Product 2" class="nitted-rotating-image" data-index="1" />
        <img src="https://images.unsplash.com/photo-1576566588028-4147f3842f27?auto=format&fit=crop&w=400&q=80" alt="Product 3" class="nitted-rotating-image" data-index="2" />
        <img src="https://images.unsplash.com/photo-1521572163474-6864f9cf17ab?auto=format&fit=crop&w=400&q=80" alt="Product 4" class="nitted-rotating-image" data-index="3" />
        
        <!-- Navigation dots -->
        <div class="nitted-dots">
          <div class="nitted-dot active" data-index="0"></div>
          <div class="nitted-dot" data-index="1"></div>
          <div class="nitted-dot" data-index="2"></div>
          <div class="nitted-dot" data-index="3"></div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
// Countdown Timer to August 1, 2025
(function() {
  function pad(n) { return n < 10 ? '0' + n : n; }
  function updateCountdown() {
    var target = new Date('2025-08-01T00:00:00Z');
    var now = new Date();
    var diff = target - now;
    if (diff < 0) diff = 0;
    var days = Math.floor(diff / (1000 * 60 * 60 * 24));
    var hours = Math.floor((diff / (1000 * 60 * 60)) % 24);
    var minutes = Math.floor((diff / (1000 * 60)) % 60);
    var seconds = Math.floor((diff / 1000) % 60);
    document.getElementById('nitted-days').textContent = pad(days);
    document.getElementById('nitted-hours').textContent = pad(hours);
    document.getElementById('nitted-minutes').textContent = pad(minutes);
    document.getElementById('nitted-seconds').textContent = pad(seconds);
  }
  updateCountdown();
  setInterval(updateCountdown, 1000);
})();

// Rotating Product Images
(function() {
  const images = document.querySelectorAll('.nitted-rotating-image');
  const dots = document.querySelectorAll('.nitted-dot');
  let currentIndex = 0;
  const totalImages = images.length;

  function showImage(index) {
    // Hide all images
    images.forEach(img => img.classList.remove('active'));
    dots.forEach(dot => dot.classList.remove('active'));
    
    // Show current image and dot
    images[index].classList.add('active');
    dots[index].classList.add('active');
  }

  function nextImage() {
    currentIndex = (currentIndex + 1) % totalImages;
    showImage(currentIndex);
  }

  // Auto-rotate every 3 seconds
  setInterval(nextImage, 3000);

  // Click on dots to manually change image
  dots.forEach((dot, index) => {
    dot.addEventListener('click', () => {
      currentIndex = index;
      showImage(currentIndex);
    });
  });
})();
</script>

