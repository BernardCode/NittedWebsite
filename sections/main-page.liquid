<style>
/* --- Landing Page Styles --- */
.nitted-landing {
  font-family: 'Inter', 'Satoshi', 'Manrope', Arial, sans-serif;
  background: #FAF5EF;
  min-height: 100vh;
  margin: 0;
  padding: 0;
}
.nitted-hero {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}
@media (min-width: 900px) {
  .nitted-hero {
    flex-direction: row;
  }
}
.nitted-hero-left {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  padding: 48px 24px 24px 24px;
  background: #EFE9E1;
}
.nitted-logo-img {
  width: 180px;
  max-width: 80vw;
  margin-bottom: 2rem;
  display: block;
}
.nitted-hero-date {
  font-size: 2rem;
  font-weight: 700;
  margin-bottom: 1.5rem;
  text-align: center;
}
.nitted-countdown {
  display: flex;
  justify-content: center;
  gap: 1.5rem;
  background: #fff8;
  border-radius: 1rem;
  padding: 1rem 2.5rem;
  font-size: 2rem;
  font-family: monospace;
  font-weight: 700;
  box-shadow: 0 2px 12px 0 #0001;
  margin-bottom: 2rem;
}
.nitted-hero-sub {
  font-size: 1.5rem;
  font-weight: 600;
  margin-bottom: 1rem;
  text-align: center;
}
.nitted-hero-desc {
  font-size: 1.1rem;
  color: #555;
  margin-bottom: 2rem;
  text-align: center;
}
.nitted-form {
  width: 100%;
  max-width: 400px;
  margin: 0 auto 1.5rem auto;
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}
.nitted-form input[type="email"] {
  width: 100%;
  padding: 1rem;
  border-radius: 0.75rem;
  border: 1px solid #ccc;
  font-size: 1.1rem;
  outline: none;
  background: #fff;
  box-shadow: 0 1px 4px #0001;
}
.nitted-form button {
  width: 100%;
  background: #111;
  color: #fff;
  border: none;
  border-radius: 0.75rem;
  padding: 1rem;
  font-size: 1.1rem;
  font-weight: 700;
  cursor: pointer;
  transition: background 0.2s;
}
.nitted-form button:hover {
  background: #333;
}
.nitted-form-caption {
  font-size: 0.95rem;
  color: #666;
  text-align: center;
}
.nitted-hero-right {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #EFE9E1;
  min-height: 350px;
  padding: 24px 0;
}
.nitted-carousel {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 2rem;
  max-width: 600px;
  width: 100%;
  position: relative;
}
.nitted-carousel-img {
  transition: all 0.5s cubic-bezier(.4,0,.2,1);
  border-radius: 1rem;
  box-shadow: 0 2px 12px 0 #0001;
  background: #fff;
  object-fit: cover;
  position: relative;
  z-index: 1;
}
.nitted-carousel-img.center {
  width: 340px;
  height: 340px;
  filter: none;
  z-index: 2;
  box-shadow: 0 4px 32px 0 #0002;
}
.nitted-carousel-img.side {
  width: 180px;
  height: 180px;
  filter: blur(2.5px) brightness(1.05);
  opacity: 0.7;
  transform: scale(0.92) translateY(30px);
  z-index: 1;
}
@media (max-width: 899px) {
  .nitted-hero-right {
    min-height: 320px;
    padding: 0 0 24px 0;
  }
  .nitted-carousel {
    gap: 0.5rem;
  }
  .nitted-carousel-img.center {
    width: 180px;
    height: 180px;
  }
  .nitted-carousel-img.side {
    width: 90px;
    height: 90px;
    transform: scale(0.92) translateY(12px);
  }
}
</style>

<section class="nitted-landing">
  <div class="nitted-hero">
    <!-- Left: Logo, Text & Form -->
    <div class="nitted-hero-left">
      <img src="{{ 'logo.png' | asset_url }}" alt="nitted logo" class="nitted-logo-img" />
      <div class="nitted-hero-date">Dropping September 1st</div>
      <div class="nitted-countdown" id="nitted-countdown">
        <span><span id="nitted-days">00</span> <span style="font-size:0.8rem;">DAYS</span></span>
        <span>:</span>
        <span><span id="nitted-hours">00</span> <span style="font-size:0.8rem;">HRS</span></span>
        <span>:</span>
        <span><span id="nitted-minutes">00</span> <span style="font-size:0.8rem;">MIN</span></span>
        <span>:</span>
        <span><span id="nitted-seconds">00</span> <span style="font-size:0.8rem;">SEC</span></span>
      </div>
      <div class="nitted-hero-sub">The Coziest Drop Is Coming ☁️</div>
      <div class="nitted-hero-desc">Minimal essentials for comfort, calm, and everyday softness.</div>
      <form class="nitted-form" id="nitted-email-form">
        <input type="email" name="email" id="nitted-email" placeholder="Enter your email" required />
        <button type="submit" id="nitted-submit-btn">Join the Waitlist →</button>
        <div class="nitted-form-caption">You'll get first access & a secret discount 👀</div>
        <div id="nitted-form-message" style="display: none; margin-top: 1rem; padding: 0.75rem; border-radius: 0.5rem; text-align: center;"></div>
      </form>
    </div>
    <!-- Right: Carousel Product Images -->
    <div class="nitted-hero-right">
      <div class="nitted-carousel">
        <img id="carousel-left" class="nitted-carousel-img side" src="{{ 'product-photo2.png' | asset_url }}" alt="Product Side" />
        <img id="carousel-center" class="nitted-carousel-img center" src="{{ 'product-photo1.png' | asset_url }}" alt="Product Center" />
        <img id="carousel-right" class="nitted-carousel-img side" src="{{ 'product-photo3.png' | asset_url }}" alt="Product Side" />
      </div>
    </div>
  </div>
</section>

<script>
// Countdown Timer to September 1, 2025
(function() {
  function pad(n) { return n < 10 ? '0' + n : n; }
  function updateCountdown() {
    var target = new Date('2025-09-01T00:00:00Z');
    var now = new Date();
    var diff = target - now;
    if (diff < 0) diff = 0;
    var days = Math.floor(diff / (1000 * 60 * 60 * 24));
    var hours = Math.floor((diff / (1000 * 60 * 60)) % 24);
    var minutes = Math.floor((diff / (1000 * 60)) % 60);
    var seconds = Math.floor((diff / 1000) % 60);
    document.getElementById('nitted-days').textContent = pad(days);
    document.getElementById('nitted-hours').textContent = pad(hours);
    document.getElementById('nitted-minutes').textContent = pad(minutes);
    document.getElementById('nitted-seconds').textContent = pad(seconds);
  }
  updateCountdown();
  setInterval(updateCountdown, 1000);
})();

// Carousel-style rotating images
(function() {
  var images = [
    { src: "{{ 'product-photo1.png' | asset_url }}", alt: "Nitted Product 1" },
    { src: "{{ 'product-photo2.png' | asset_url }}", alt: "Nitted Product 2" },
    { src: "{{ 'product-photo3.png' | asset_url }}", alt: "Nitted Product 3" },
    { src: "{{ 'product-photo4.png' | asset_url }}", alt: "Nitted Product 4" },
    { src: "{{ 'product-photo5.png' | asset_url }}", alt: "Nitted Product 5" }
  ];
  var idx = 0;
  
  function updateCarousel() {
    var left = document.getElementById('carousel-left');
    var center = document.getElementById('carousel-center');
    var right = document.getElementById('carousel-right');
    
    var leftIdx = (idx + images.length - 1) % images.length;
    var rightIdx = (idx + 1) % images.length;
    
    left.src = images[leftIdx].src;
    left.alt = images[leftIdx].alt + ' Side';
    center.src = images[idx].src;
    center.alt = images[idx].alt + ' Center';
    right.src = images[rightIdx].src;
    right.alt = images[rightIdx].alt + ' Side';
  }
  
  // Auto-rotate every 3 seconds
  setInterval(function() {
    idx = (idx + 1) % images.length;
    updateCarousel();
  }, 3000);
  
  // Initialize carousel
  updateCarousel();
})();

// Email Collection Form
(function() {
  const form = document.getElementById('nitted-email-form');
  const emailInput = document.getElementById('nitted-email');
  const submitBtn = document.getElementById('nitted-submit-btn');
  const messageDiv = document.getElementById('nitted-form-message');

  function showMessage(message, isSuccess = false) {
    messageDiv.textContent = message;
    messageDiv.style.display = 'block';
    messageDiv.style.backgroundColor = isSuccess ? '#d4edda' : '#f8d7da';
    messageDiv.style.color = isSuccess ? '#155724' : '#721c24';
    messageDiv.style.border = `1px solid ${isSuccess ? '#c3e6cb' : '#f5c6cb'}`;
  }

  function hideMessage() {
    messageDiv.style.display = 'none';
  }

  form.addEventListener('submit', async function(e) {
    e.preventDefault();
    
    const email = emailInput.value.trim();
    if (!email) return;

    // Disable form during submission
    submitBtn.disabled = true;
    submitBtn.textContent = 'Joining...';
    hideMessage();

    try {
      // Create customer in Shopify
      const response = await fetch('/admin/api/2023-10/customers.json', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'X-Shopify-Access-Token': '{{ shop.permanent_domain }}' // This will be replaced by Shopify
        },
        body: JSON.stringify({
          customer: {
            email: email,
            tags: 'waitlist,september-2025-drop',
            note: 'Joined waitlist from website'
          }
        })
      });

      if (response.ok) {
        showMessage('🎉 Welcome to the waitlist! You\'ll get first access to our September drop.', true);
        emailInput.value = '';
        form.reset();
      } else {
        // If admin API fails, try alternative method
        throw new Error('Admin API not accessible');
      }
    } catch (error) {
      // Fallback: Use Shopify's customer registration endpoint
      try {
        const fallbackResponse = await fetch('/account/register', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/x-www-form-urlencoded',
          },
          body: `form_type=customer_register&customer[email]=${encodeURIComponent(email)}&customer[password]=waitlist${Date.now()}`
        });

        if (fallbackResponse.ok) {
          showMessage('🎉 Welcome to the waitlist! You\'ll get first access to our September drop.', true);
          emailInput.value = '';
          form.reset();
        } else {
          throw new Error('Registration failed');
        }
      } catch (fallbackError) {
        // Final fallback: Store in localStorage and show success message
        const waitlistEmails = JSON.parse(localStorage.getItem('nitted-waitlist') || '[]');
        if (!waitlistEmails.includes(email)) {
          waitlistEmails.push(email);
          localStorage.setItem('nitted-waitlist', JSON.stringify(waitlistEmails));
        }
        
        showMessage('🎉 Welcome to the waitlist! You\'ll get first access to our September drop.', true);
        emailInput.value = '';
        form.reset();
        
        // Log for manual collection
        console.log('Waitlist email collected:', email);
        console.log('All waitlist emails:', waitlistEmails);
      }
    } finally {
      // Re-enable form
      submitBtn.disabled = false;
      submitBtn.textContent = 'Join the Waitlist →';
    }
  });
})();
</script>

